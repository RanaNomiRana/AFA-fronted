import React from 'react';
import '../App.css'; // Import CSS file

const Navbar: React.FC = () => {
    return (
        <nav className="navbar p-5 d-flex justify-content-center">
            <h1 className="title">Data Analysis Dashboard</h1>
        </nav>
    );
}

export default Navbar;
